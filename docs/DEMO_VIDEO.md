# Three-minute demo outline

1. **0:00–1:00 — Working product:** load the RISC-V example, run simulation, inspect the waveform, and trigger Auto-Fix on a deliberate failure.
2. **1:00–3:00 — Technical implementation:** screen-split the working app with the WASM wrapper, VCD renderer, and Auto-Fix streaming code. Explain the binary VCD parsing, viewport rendering, failure-context packaging, and one-cycle branch-hazard fix.

Show the featured Codex session ID and a corresponding diary screenshot before the final call to action.

